# CosmiKAI — Python dependencies
# Install with: pip install -r requirements.txt

# ── Required by main code/ ─────────────────────────────────────────────────
lightkurve        # Used in data_ingestion.py (MAST light curve download)
astropy           # Used in preprocessing.py (BoxLeastSquares) and units in data_ingestion.py
numpy             # Used throughout main code/
torch             # Used in model_inference.py

# ── Optional: non-main-code components ─────────────────────────────────────
scipy             # Used by other project modules, not required by main code/
fastapi           # Backend API framework
uvicorn[standard] # ASGI server runtime for backend
aiosqlite         # Async SQLite driver for backend cache
pydantic          # Backend schema validation
